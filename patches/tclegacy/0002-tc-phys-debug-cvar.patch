From 0000000000000000000000000000000000000000 Mon Sep 17 00:00:00 2001
Subject: [PATCH 2/2] tc: add tc_phys_debug cvar for server-side logging

Registers a server cvar `tc_phys_debug` to control physics logs.

---
 src/game/g_main.c | 10 ++++++++++
 1 file changed, 10 insertions(+ )

--- a/src/game/g_main.c
+++ b/src/game/g_main.c
@@
+static vmCvar_t tc_phys_debug;
@@ void G_InitGame( int levelTime, int randomSeed, qboolean restart ) {
+    trap_Cvar_Register( &tc_phys_debug, "tc_phys_debug", "0", CVAR_ARCHIVE|CVAR_SERVERINFO );
@@ void G_RunFrame( int levelTime ) {
+    if ( tc_phys_debug.integer ) {
+        // placeholder: later show active phys entities, timings, etc.
+        G_Printf( "[TC] phys debug frame %d\n", levelTime );
+    }
